# Trajectory Schema Validation Report

> Generated by `scripts/validate_trajectory_schema.py`.

## 1. Run parameters

```
input    : data/trajectories/all_schedules_merged.jsonl
max_rows : ALL
checked  : 7500
```

## 2. Required-field coverage

- rows with all 15 required fields : **7500 / 7500**

## 3. Shape checks

- rows failing `len(state_features) == 5`           : **0**
- rows failing per-candidate `len == 10`              : **0**
- rows failing `len(cands)==len(keys)==len(mask)`             : **0**
- rows failing `0 <= expert_action_index < num_candidates`    : **0**

## 4. SKIP convention

- rows with `candidate_keys[0].is_skip == True`               : **7500 / 7500**
- rows failing the SKIP convention                            : **0**

✅ The exporter **already prepends a SKIP candidate at index 0**, with `candidate_features[0][0] == 1.0` as the `is_skip` flag.

Therefore the training dataloader **MUST NOT prepend another SKIP**. When it subsamples candidates, it must:

- always keep candidate at index 0 (the SKIP slot);
- always keep the expert candidate (the one at `expert_action_index`);
- remap the expert label to its new position after subsampling.

## 5. Label distribution

- `expert_action_index == 0` (SKIP)            : **714 (9.5%)**
    - of which `_debug.expert_skipped_by_json` : 714
    - of which `_debug.expert_infeasible_in_replay` : 0
- `expert_action_index >= 1` (positive action) : **6786 (90.5%)**

## 6. Candidate-set size

- `num_candidates_incl_skip` min  = 1
- `num_candidates_incl_skip` mean = 978.3
- `num_candidates_incl_skip` max  = 4291

## 8. Verdict

**PASS** - schema valid; ready to train BC.
