# EOS-Bench / CCOD 研究进展

> 本文件是持续更新的研究与工程日志。每个阶段均记录目标、实现、证据、风险与下一步；只有满足验收门槛的工作才标记为完成。

## 总体目标

在单台 Mac 可完成的数据与诊断阶段，先验证 counterfactual action ranking 是否存在稳定、可学习且能改善完整调度的信号；只有本地诊断通过后，才租用 RTX 3090 进行短时训练。

---

## 进展 001：方案冻结与本地运行基线

日期：2026-07-15  
状态：已完成

### 完成内容

1. 新增资源受限版研究方案 `方案_v2.md`，明确了 P0 correctness、10-state exact replay、100-state diagnostic、Mac/3090 分工与 go/no-go 门槛。
2. 固定 Python 3.10.20 与核心依赖版本，并创建本地 `.venv`。
3. 确认当前 Mac 上 PyTorch 可运行，但 MPS 不可用，因此当前阶段以 CPU 为准。

### 对应提交

- `22b7d33`：`docs: add resource-aware CCOD research plan`
- `e6cdcac`：`build: pin Python 3.10 runtime dependencies`

### 验收证据

- `python -m pip check`：通过。
- 本地环境无需 GPU 即可运行 schema、回放与小规模诊断测试。

---

## 进展 002：轨迹语义与约束正确性修复

日期：2026-07-15  
状态：已完成

### 完成内容

1. 将 agility profile 与 non-agile transition 参数写入调度元数据，避免生成与评估约束不一致。
2. 修正 class-2 implicit objective 映射并增加 fail-fast 校验。
3. 将轨迹目标统一为同一 schedule objective 下的状态增量：

$$
\Delta F_t = F(S_{t+1}) - F(S_t).
$$

4. 对 SKIP 使用同一转移定义；SKIP 不再混入额外手工惩罚。
5. schema v2 增加 `objective_delta` 与 `observed_return_H`，并保留 legacy 数据验证能力。

### 对应提交

- `237742d`：`fix: make trajectory metadata self-consistent`
- `3fe014b`：`fix: unify trajectory targets with schedule objective`

### 验收证据

- 真实 500 步轨迹通过 schema v2 validator。
- 其中 490 个 SKIP 的 `objective_delta` 全部为 0。
- 全部增量之和为 0.02，与最终 completion score 一致。

---

## 进展 003：Exact Prefix Replay

日期：2026-07-15  
状态：已完成，等待写入对应提交号

### 完成内容

1. 定义显式 SKIP 与 assignment 的稳定 ActionKey。
2. 使用 scenario-relative 整数微秒编码时间，避免浮点时间身份漂移。
3. 定义 canonical task order、candidate order、prefix hash、schedule hash、candidate membership/order hash 与 state hash。
4. 从原 scenario 和 action prefix 重新枚举并恢复 Assignment，不从有损 schedule JSON 手工拼装状态。
5. trace provenance 记录 Git commit、关键实现文件 SHA-256 与 dirty 状态；代码来源不混入跨 solver 去重所用的物理状态身份。
6. smoke 子进程从输出目录启动，验证回放不依赖仓库工作目录。

### 当前验收证据

- 单元测试：29/29 通过。
- 真实 Sats1/M100 场景：10/10 个分层状态在两个全新 Python 进程中完全一致。
- 比较项包括 prefix、schedule、ordered candidate set、candidate membership、physical state、objective 与 state hash。
- 最大抽查候选集合为 1,538 个；恢复过程未修改源 schedule。
- 低 candidate cap 下打乱观测/通信窗口输入顺序，规范候选前缀保持一致。
- SKIP 前后 schedule hash 保持不变，但包含决策游标的 physical/state hash 正确变化。
- 新进程从输出目录而非仓库根目录启动，回放仍完全一致。

### 阶段边界

本阶段只提交 exact replay 相关文件与本日志，不混入用户原先暂存的 `方案.md`。真正的 CCOD 算法将在独立目录 `algorithms/ccod/` 中实现。

---

## 下一阶段

1. 完成 CCOD novelty 的三组跨源论文检索与 scoop check。
2. 重写 `方案_v2.md` 的摘要、主要创新、相关工作、参考文献与公式格式。
3. 在独立目录 `algorithms/ccod/` 实现 `force_action(s,a)` 与确定性 objective-greedy `H=5` continuation。
4. 在 Mac CPU 上运行 100-state diagnostic；未通过信号门槛前不租 3090。
